# Examples

Recurring bug pattern log. Append new entries as they're found — see
`guidelines.md` § Threshold for when a bug is worth adding here vs just
fixing it.

| Bug Pattern | How It Hides | How to Catch It |
|---|---|---|
| Silent skip when an error was expected | Looks like a successful no-op | Test the case where the expected entry/dependency is missing entirely |
| Wrong error category returned (e.g. server error instead of client error) | Both surface as "an error happened" | Verify the specific error code/category through the actual error-handling layer |
| Nil/empty dependency data not handled | Works in the common case, breaks when a dependency legitimately returns nothing | Explicitly test with the dependency returning empty/null |
| Combined-change scenarios not tested (one field that should trigger re-validation + one that shouldn't, changed together) | Single-field tests all pass individually | Test scenarios where a "should trigger" and "should not trigger" change happen in the same request |
| A stack-specific "fix" (e.g. adding `ShouldQueue`) changes async/delivery behavior without re-checking the infra assumption the original design decision was based on | The change looks like a strict improvement in isolation, and the test suite's own environment config (e.g. a hardcoded sync queue override) makes the regression invisible to the test runner | Diff the change against the techplan's Decision Log for any entry it touches, and check the *real* env/infra config (not just the test config) for the assumption that decision was conditioned on |
| `SELECT ... FOR UPDATE` used to serialize "invalidate old, insert new" logic locks nothing when the query matches zero rows, so two concurrent first-time inserts both succeed | Passes every functional/sequential test (there's always a "before" row to lock when tests run one request at a time); only manifests when two requests race with no pre-existing row to lock | Explicitly test the *zero-prior-state* concurrent case, not just "two requests when one valid row already exists" — run the exact SQL/logic with two overlapping transactions/sessions against an empty starting state |
| A test asserts "0 rows" in a DB-backed session/state table as proof an action (e.g. force-logout) fully invalidated everything for that owner, but the framework itself re-creates a row tagged with the same owner column at the end of the very request under test (e.g. a session store re-persisting a fresh, empty, un-authenticated row at end-of-request, stamped with the still-resolved `user_id`) | Passes for any request path that happens to skip the framework machinery that recreates the row (e.g. a stateless API call with no session middleware in that path); only fails once the identical logic is exercised from a route that always runs that machinery — reads as environment-flaky rather than logically wrong | Before trusting a table-wide row-count assertion, decode what the leftover row actually contains (is it functionally live, or an inert artifact of framework bookkeeping); prefer a functional replay of the specific prior identifier (old cookie/token) over a bare count whenever the invalidating request itself runs through the same middleware that persists that table |
| A test deliberately forces a genuine (un-savepointed) DB-level exception — e.g. racing a real UNIQUE-constraint violation past an app-level pre-check to exercise a `catch`-and-convert backstop — inside a test-framework-wrapped outer transaction (Laravel `RefreshDatabase` and equivalents in other stacks). The app itself catches and converts the exception correctly, but the *test's own* enclosing transaction is left aborted for the rest of that test | The response/assertion right after the triggering call still passes (the app-level catch worked); only a later, unrelated-looking follow-up query on the same connection in the same test — e.g. a belt-and-suspenders DB re-read to double-check state — throws (Postgres: `SQLSTATE[25P02]`), which reads like flaky infrastructure rather than a direct, deterministic consequence of the exception the test intentionally caused two lines earlier | After deliberately forcing a real DB-level exception inside a transaction-wrapped test, treat the response body as the sole source of truth for the rest of that test — the database's own atomicity already guarantees the failed statement had zero effect, so a redundant re-read on the now-poisoned connection is both unnecessary and the thing that actually breaks |
